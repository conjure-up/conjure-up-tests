#!/bin/bash

set -eux

. vars

sudo -E su "$USER" -c "conjure-up -c $CONJUREUP_TESTDIR/deployments/Conjurefile.localhost.base -c $CONJUREUP_TESTDIR/deployments/Conjurefile.localhost.openstack-novalxd"
echo $?
