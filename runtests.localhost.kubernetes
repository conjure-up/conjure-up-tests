#!/bin/bash

set -eux

. vars

sudo -E su "$USER" -c "source $CONJUREUP_BASEDIR/conjure-dev/bin/activate && conjure-up -c $CONJUREUP_TESTDIR/deployments/Conjurefile.localhost.base -c $CONJUREUP_TESTDIR/deployments/Conjurefile.localhost.canonical-kubernetes" && cat ~/.cache/conjure-up/conjure-up.log
exit $?
